# Use the official Nginx image as a base for serving static content
FROM nginx:alpine

# Copy the HTML file from the local directory into the container
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80 to access the frontend through the web server
EXPOSE 80

